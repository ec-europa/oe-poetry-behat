language: php

php:
  - 5.6
  - 7

env:
  global:
    - PATH=$PATH:/home/travis/.composer/vendor/bin

install:
  - composer self-update
  - composer install

script:
  - ./vendor/bin/grumphp run
  - ./vendor/bin/behat

notifications:
  email: false
